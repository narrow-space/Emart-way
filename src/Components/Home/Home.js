import React from 'react';
import Products from '../Products/Products';
import { useSelector } from 'react-redux';
const Home = () => {
  
    return (
        <div>
         <Products></Products>
        </div>
    );
};

export default Home;